//% color=#ff8800 icon="\uf108" block="Arcade UI Tools"
namespace arcadeUI {

    /**
     * Apply fullscreen mode and transparent buttons in the simulator
     */
    //% block="apply fullscreen simulator and transparent buttons"
    export function applyFullscreenAndTransparentButtons() {
        // This function doesn't need to do anything.
        // The simulator-side code in sim.ts runs automatically.
    }
}
